<%= form_with(model: set, local: true) do |form| %>
  <% if set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(set.errors.count, "error") %> prohibited this set from being saved:</h2>

      <ul>
      <% set.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
